const request = require('request');
const serModule = require('./Server/server');
let inputData;
let dataForSearch;
//callMealDB.callMealDB('search.php?s=Arrabiata');
inputData = serModule.server(inputData, dataForSearch);
